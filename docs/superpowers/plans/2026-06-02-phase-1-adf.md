# Phase 1 ADF Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the course-compliant repository skeleton and Phase 1 architecture driver documents for the IoT smart warehouse project.

**Architecture:** This plan does not implement runtime services yet. It establishes the project structure and the architecture driver foundation that later ADRs, C4 views, PoC reports, and prototype services must trace back to.

**Tech Stack:** Markdown, Mermaid-compatible architecture documentation, PowerShell verification commands, Git.

---

## File Structure

- Create: `README.md` - project overview, scope, quick navigation, and future startup command location.
- Create: `docs/01_adf/ADF_Brief.md` - architecture driver brief required by Phase 1.
- Create: `docs/01_adf/Quality_Scenarios.md` - at least 8 quality attribute scenarios in six-element format.
- Create: `docs/02_views/.gitkeep` - reserve C4 view directory for Phase 2.
- Create: `docs/03_adrs/.gitkeep` - reserve ADR directory for Phase 2.
- Create: `docs/04_evaluation/.gitkeep` - reserve ATAM and risk directory for Phase 3.
- Create: `docs/05_evidence/.gitkeep` - reserve validation evidence directory for Phase 4.
- Create: `docs/06_evolution/.gitkeep` - reserve evolution roadmap directory for Phase 5.
- Create: `docs/07_runbook/.gitkeep` - reserve setup and demo runbook directory.
- Create: `contracts/openapi/.gitkeep` - reserve REST API contract directory.
- Create: `contracts/events/.gitkeep` - reserve event JSON Schema directory.
- Create: `scripts/data-gen/.gitkeep` - reserve device data generator directory.
- Create: `scripts/load-test/.gitkeep` - reserve load test script directory.
- Create: `scripts/fault-injection/.gitkeep` - reserve fault injection script directory.
- Create: `apps/device-gateway/.gitkeep` - reserve edge gateway service directory.
- Create: `apps/inventory-service/.gitkeep` - reserve inventory and device shadow service directory.
- Create: `apps/alert-service/.gitkeep` - reserve alert service directory.
- Create: `libs/.gitkeep` - reserve shared library directory.

## Task 1: Create Course Project Skeleton

**Files:**
- Create: `README.md`
- Create: `docs/01_adf/`
- Create: `docs/02_views/.gitkeep`
- Create: `docs/03_adrs/.gitkeep`
- Create: `docs/04_evaluation/.gitkeep`
- Create: `docs/05_evidence/.gitkeep`
- Create: `docs/06_evolution/.gitkeep`
- Create: `docs/07_runbook/.gitkeep`
- Create: `contracts/openapi/.gitkeep`
- Create: `contracts/events/.gitkeep`
- Create: `scripts/data-gen/.gitkeep`
- Create: `scripts/load-test/.gitkeep`
- Create: `scripts/fault-injection/.gitkeep`
- Create: `apps/device-gateway/.gitkeep`
- Create: `apps/inventory-service/.gitkeep`
- Create: `apps/alert-service/.gitkeep`
- Create: `libs/.gitkeep`

- [ ] **Step 1: Create directories**

Run:

```powershell
$dirs = @(
  'docs/01_adf',
  'docs/02_views',
  'docs/03_adrs',
  'docs/04_evaluation',
  'docs/05_evidence',
  'docs/06_evolution',
  'docs/07_runbook',
  'contracts/openapi',
  'contracts/events',
  'scripts/data-gen',
  'scripts/load-test',
  'scripts/fault-injection',
  'apps/device-gateway',
  'apps/inventory-service',
  'apps/alert-service',
  'libs'
)
foreach ($dir in $dirs) {
  New-Item -ItemType Directory -Force -Path $dir | Out-Null
}
```

Expected: command exits with code 0 and creates all listed directories.

- [ ] **Step 2: Add `.gitkeep` files**

Run:

```powershell
$files = @(
  'docs/02_views/.gitkeep',
  'docs/03_adrs/.gitkeep',
  'docs/04_evaluation/.gitkeep',
  'docs/05_evidence/.gitkeep',
  'docs/06_evolution/.gitkeep',
  'docs/07_runbook/.gitkeep',
  'contracts/openapi/.gitkeep',
  'contracts/events/.gitkeep',
  'scripts/data-gen/.gitkeep',
  'scripts/load-test/.gitkeep',
  'scripts/fault-injection/.gitkeep',
  'apps/device-gateway/.gitkeep',
  'apps/inventory-service/.gitkeep',
  'apps/alert-service/.gitkeep',
  'libs/.gitkeep'
)
foreach ($file in $files) {
  if (-not (Test-Path $file)) {
    New-Item -ItemType File -Path $file | Out-Null
  }
}
```

Expected: command exits with code 0 and each reserved directory has a `.gitkeep` file.

- [ ] **Step 3: Create `README.md`**

Write this content to `README.md`:

```markdown
# IoT 智能仓储监控与告警平台

本项目是《软件体系结构》课程期末大作业选题二“物联网智能仓储管理系统”的架构原型与文档工程。

## 项目目标

项目聚焦物流仓储场景，通过模拟温湿度传感器、重量传感器、RFID 扫码设备和 AGV 状态设备，验证 IoT 设备接入、边云协同、设备影子、时序数据存储、事件驱动告警和边缘缓存补传等关键架构决策。

本项目不实现完整 WMS 或 ERP 系统。代码原型只用于证明架构决策可运行、可验证、可追溯。

## 当前阶段

当前正在完成 Phase 1：架构驱动因素。

- `docs/01_adf/ADF_Brief.md`：架构驱动因素简报
- `docs/01_adf/Quality_Scenarios.md`：质量属性场景清单
- `docs/superpowers/specs/2026-06-02-iot-smart-warehouse-design.md`：已确认的设计文档
- `docs/superpowers/plans/2026-06-02-phase-1-adf.md`：当前实施计划

## 计划中的原型服务

- `apps/device-gateway/`：边缘设备网关，负责 MQTT 消息接入、校验、缓存和转发。
- `apps/inventory-service/`：库存与设备影子服务，负责设备状态、库存状态和补货事件。
- `apps/alert-service/`：告警服务，负责消费异常事件并生成告警记录。

## 计划中的本地环境

后续原型将通过 `docker-compose up` 一键启动，包含 MQTT Broker、TimescaleDB 和三个 FastAPI 服务。
```

Expected: `README.md` contains a clear project overview and points readers to Phase 1 documents.

- [ ] **Step 4: Verify skeleton**

Run:

```powershell
$required = @(
  'README.md',
  'docs/01_adf',
  'docs/02_views/.gitkeep',
  'docs/03_adrs/.gitkeep',
  'docs/04_evaluation/.gitkeep',
  'docs/05_evidence/.gitkeep',
  'docs/06_evolution/.gitkeep',
  'docs/07_runbook/.gitkeep',
  'contracts/openapi/.gitkeep',
  'contracts/events/.gitkeep',
  'scripts/data-gen/.gitkeep',
  'scripts/load-test/.gitkeep',
  'scripts/fault-injection/.gitkeep',
  'apps/device-gateway/.gitkeep',
  'apps/inventory-service/.gitkeep',
  'apps/alert-service/.gitkeep',
  'libs/.gitkeep'
)
$missing = $required | Where-Object { -not (Test-Path $_) }
if ($missing) {
  $missing
  exit 1
}
'skeleton ok'
```

Expected: prints `skeleton ok`.

- [ ] **Step 5: Commit skeleton**

Run:

```powershell
git add README.md docs/02_views/.gitkeep docs/03_adrs/.gitkeep docs/04_evaluation/.gitkeep docs/05_evidence/.gitkeep docs/06_evolution/.gitkeep docs/07_runbook/.gitkeep contracts/openapi/.gitkeep contracts/events/.gitkeep scripts/data-gen/.gitkeep scripts/load-test/.gitkeep scripts/fault-injection/.gitkeep apps/device-gateway/.gitkeep apps/inventory-service/.gitkeep apps/alert-service/.gitkeep libs/.gitkeep
git commit -m "chore: add course project skeleton"
```

Expected: Git creates a commit named `chore: add course project skeleton`.

## Task 2: Write Architecture Driver Brief

**Files:**
- Create: `docs/01_adf/ADF_Brief.md`

- [ ] **Step 1: Create `ADF_Brief.md`**

Write this content to `docs/01_adf/ADF_Brief.md`:

```markdown
# ADF Brief - IoT 智能仓储监控与告警平台

## 1. 项目概述

某物流公司希望为仓储网络建设一套 IoT 驱动的智能仓储平台，统一接入仓库中的温湿度传感器、重量传感器、RFID 扫码设备和 AGV 状态设备。系统需要持续采集设备数据，维护设备与库存状态，并在温度、湿度、重量或设备状态异常时快速生成告警。

本项目采用边云协同架构：边缘侧负责设备接入、基础校验、短期缓存和恢复补传；云端负责设备影子、库存状态、告警记录、长期存储和 API 查询。可运行原型不接入真实硬件，而是使用模拟设备数据验证关键架构决策。

项目目标不是实现完整 WMS 或 ERP，而是形成可追溯、可验证、可演进的架构设计闭环。

## 2. 核心用户角色

| 用户角色 | 说明 | 主要关注点 |
| --- | --- | --- |
| 仓库管理员 | 负责查看仓库设备状态、库存状态和异常告警 | 告警及时、库存状态可信、操作简单 |
| 运维人员 | 负责设备接入、服务部署、故障排查和容量规划 | 可观测性、可靠性、可恢复性 |
| 物流运营人员 | 关注库存变化、补货事件和仓储运行效率 | 库存数据准确、补货事件可追踪 |
| IoT 设备 | 温湿度传感器、重量传感器、RFID 设备和 AGV 状态设备 | 低开销上报、网络异常时不丢关键数据 |
| 外部 WMS/ERP 系统 | 未来可对接库存和补货事件 | 接口清晰、数据一致、权限可控 |

## 3. 系统边界

### 范围内

- 模拟 IoT 设备上报温湿度、重量、RFID 和 AGV 状态数据。
- 使用 MQTT Broker 接收设备消息。
- 使用边缘网关完成消息校验、缓存和转发。
- 维护设备影子，保存设备最后一次已知状态。
- 维护简化库存状态，并在库存低于阈值时触发补货事件。
- 在传感器数据超过阈值时生成异常告警。
- 提供 REST API 查询设备状态、库存状态和告警记录。
- 提供 Docker Compose 本地演示环境。

### 范围外

- 不接入真实硬件设备。
- 不实现真实 AGV 路径规划。
- 不实现完整 WMS、ERP 或财务系统。
- 不开发复杂前端管理后台。
- 不调用真实短信、电话、邮件或企业微信告警通道。
- 不实现完整 OTA 固件升级流程，OTA 只作为架构演进能力进行设计。

## 4. 功能性需求摘要

### FR-001 设备数据上报

As a IoT device, I want to publish telemetry data to the platform, so that warehouse status can be monitored continuously.

### FR-002 设备消息接入

As an edge gateway, I want to receive and validate device messages, so that invalid or malformed data does not enter cloud services.

### FR-003 设备影子维护

As a warehouse administrator, I want to view the latest known state of each device, so that I can understand device status even when a device is temporarily offline.

### FR-004 库存状态更新

As a logistics operator, I want RFID and weight data to update simplified inventory state, so that replenishment decisions have a data basis.

### FR-005 异常告警生成

As a warehouse administrator, I want abnormal temperature, humidity, weight, and device state changes to generate alerts, so that risks can be handled quickly.

### FR-006 自动补货事件

As a logistics operator, I want low stock levels to trigger replenishment events, so that inventory shortages can be detected early.

### FR-007 告警查询

As an operations engineer, I want to query alert records through REST APIs, so that demo, troubleshooting, and evidence collection are easy.

### FR-008 边缘缓存与补传

As an operations engineer, I want the edge gateway to cache messages when cloud services are unavailable, so that short network failures do not cause critical data loss.

## 5. 架构重要需求

| ASR 编号 | 描述 | 关联质量属性 |
| --- | --- | --- |
| ASR-001 | 系统需要支持大量设备通过轻量协议持续上报数据 | 可伸缩性、性能 |
| ASR-002 | 异常数据从设备上报到告警生成不超过 5 秒 | 实时性 |
| ASR-003 | 云端短暂不可用时，边缘侧需要缓存并恢复补传关键数据 | 可靠性 |
| ASR-004 | 系统需要维护设备影子，支持设备离线后的状态查询 | 可靠性、可维护性 |
| ASR-005 | 传感器历史数据需要按时间查询和聚合分析 | 可观测性、可维护性 |
| ASR-006 | 告警处理链路需要与设备接入链路解耦 | 可伸缩性、可修改性 |
| ASR-007 | 设备接入需要具备身份认证和防伪造策略 | 安全性 |
| ASR-008 | 本地原型需要通过 Docker Compose 一键启动 | 可部署性、可演示性 |

## 6. 质量属性优先级

采用 MoSCoW 方法排序：

| 质量属性 | 优先级 | 理由 |
| --- | --- | --- |
| 实时性 | Must Have | 题目明确要求异常告警从感知到推送不超过 5 秒，且适合 Demo 验证 |
| 可靠性 | Must Have | IoT 网络不稳定，设备离线缓存和恢复补传是题目核心挑战 |
| 可伸缩性 | Must Have | 题目要求 10,000+ 设备接入和 50,000 msg/s 消息吞吐 |
| 安全性 | Should Have | 题目要求设备双向认证和防伪造接入，原型可简化，文档必须完整设计 |
| 可维护性 | Should Have | OTA、服务边界和事件 Schema 影响后续演进能力 |
| 可观测性 | Should Have | 告警、压测和故障注入需要可观测数据支撑 |
| 用户体验 | Nice to Have | 本作业不开发复杂前端，通过 API 文档和 Demo 步骤满足展示需要 |

## 7. 约束条件

### 技术约束

- 原型使用 Python + FastAPI 实现核心服务。
- IoT 接入优先使用 MQTT，Broker 使用 Eclipse Mosquitto。
- 时序数据存储优先使用 TimescaleDB。
- 本地环境使用 Docker Compose 启动。
- 架构图使用 Mermaid 或 PlantUML 代码形式，避免截图。

### 业务约束

- 项目服务于课程期末大作业，重点是架构推理、文档闭环和可运行验证。
- 原型只实现关键行为，不实现完整业务产品。
- 压测结论需要说明本地测试环境边界，不能夸大到真实生产规模。

### 组织约束

- 团队规模按课程要求为 3 到 4 人。
- 技术栈选择以课程原型开发效率和答辩可解释性为优先。
- 每位成员需要能解释自己负责的文档、ADR、视图或代码证据。

## 8. 关键干系人与关注点

| 干系人角色 | 主要关注点 | 对应质量属性 |
| --- | --- | --- |
| 课程教师 | 架构决策是否可追溯、可验证、可演进 | 可维护性、可验证性 |
| 仓库管理员 | 告警是否及时，设备状态是否可信 | 实时性、可靠性 |
| 运维人员 | 故障是否可定位，服务是否容易恢复 | 可观测性、可靠性 |
| 物流运营人员 | 库存状态和补货事件是否可追踪 | 一致性、可审计性 |
| 安全负责人 | 是否能防止伪造设备接入 | 安全性 |
| 开发团队 | 服务边界是否清晰，原型是否可按时完成 | 可维护性、可部署性 |
```

Expected: `ADF_Brief.md` covers project overview, roles, boundaries, functional requirements, ASRs, priorities, constraints, and stakeholders.

- [ ] **Step 2: Verify required ADF sections**

Run:

```powershell
$content = Get-Content 'docs/01_adf/ADF_Brief.md' -Raw -Encoding utf8
$required = @(
  '## 1. 项目概述',
  '## 2. 核心用户角色',
  '## 3. 系统边界',
  '## 4. 功能性需求摘要',
  '## 5. 架构重要需求',
  '## 6. 质量属性优先级',
  '## 7. 约束条件',
  '## 8. 关键干系人与关注点'
)
$missing = $required | Where-Object { -not $content.Contains($_) }
if ($missing) {
  $missing
  exit 1
}
'adf brief ok'
```

Expected: prints `adf brief ok`.

- [ ] **Step 3: Commit ADF brief**

Run:

```powershell
git add docs/01_adf/ADF_Brief.md
git commit -m "docs: add architecture driver brief"
```

Expected: Git creates a commit named `docs: add architecture driver brief`.

## Task 3: Write Quality Attribute Scenarios

**Files:**
- Create: `docs/01_adf/Quality_Scenarios.md`

- [ ] **Step 1: Create `Quality_Scenarios.md`**

Write this content to `docs/01_adf/Quality_Scenarios.md`:

```markdown
# Quality Scenarios - IoT 智能仓储监控与告警平台

本文档使用质量属性场景六要素格式描述系统的关键非功能需求。每个场景都需要在后续 ADR、架构视图和验证证据中被追溯。

## 场景索引

| QAS 编号 | 场景名称 | 质量属性 | 优先级 | 关联 ADR |
| --- | --- | --- | --- | --- |
| QAS-001 | 异常温度 5 秒内告警 | 实时性 | Must Have | ADR-005 |
| QAS-002 | 设备消息高吞吐接入 | 可伸缩性 | Must Have | ADR-001, ADR-002 |
| QAS-003 | 云端短暂不可用后的边缘补传 | 可靠性 | Must Have | ADR-002, ADR-006 |
| QAS-004 | 设备离线后仍可查询最后状态 | 可靠性 | Must Have | ADR-004 |
| QAS-005 | 传感器历史数据按时间查询 | 可观测性 | Should Have | ADR-003 |
| QAS-006 | 伪造设备接入被拒绝 | 安全性 | Should Have | ADR-001 |
| QAS-007 | 告警规则变更不影响设备接入 | 可修改性 | Should Have | ADR-005 |
| QAS-008 | 批量设备升级不影响在线采集 | 可维护性 | Should Have | ADR-004, ADR-002 |
| QAS-009 | 库存低于阈值触发补货事件 | 业务及时性 | Should Have | ADR-005 |

## QAS-001：异常温度 5 秒内告警

| 要素 | 描述 |
| --- | --- |
| 刺激源 | 温湿度传感器 |
| 刺激 | 设备上报温度超过仓库安全阈值，例如 38 摄氏度 |
| 环境 | 系统处于正常运行状态，MQTT Broker、device-gateway、inventory-service 和 alert-service 均可用 |
| 制品 | 设备接入链路、设备影子、异常事件通道、告警服务 |
| 响应 | 系统接收遥测数据，更新设备影子，发布异常事件，并生成 HIGH_TEMPERATURE 告警 |
| 响应度量 | 从设备消息时间戳到告警记录创建时间不超过 5 秒 |

**关联 ADR**：ADR-005  
**优先级**：Must Have

## QAS-002：设备消息高吞吐接入

| 要素 | 描述 |
| --- | --- |
| 刺激源 | 大量仓库 IoT 设备 |
| 刺激 | 多个仓库中的传感器持续上报温湿度、重量、RFID 和 AGV 状态消息 |
| 环境 | 开学季或促销季仓储高峰，设备接入量接近设计峰值 |
| 制品 | MQTT Broker、device-gateway、事件通道 |
| 响应 | Broker 接收发布消息，device-gateway 按 Topic 消费并转发有效数据，系统通过水平扩展网关实例提升处理能力 |
| 响应度量 | 架构设计目标支持 10,000+ 设备接入和 50,000 msg/s；本地压测报告记录可达到的测试吞吐和适用边界 |

**关联 ADR**：ADR-001, ADR-002  
**优先级**：Must Have

## QAS-003：云端短暂不可用后的边缘补传

| 要素 | 描述 |
| --- | --- |
| 刺激源 | 云端服务故障或网络抖动 |
| 刺激 | device-gateway 无法将设备消息转发到 inventory-service |
| 环境 | 仓库边缘侧仍能接收设备消息，云端服务短暂不可用 |
| 制品 | device-gateway、边缘缓存、补传任务 |
| 响应 | 边缘网关将消息写入本地缓存，云端恢复后按原始时间顺序补传 |
| 响应度量 | 云端恢复后 60 秒内补传缓存中的关键消息，补传过程中消息不重复处理 |

**关联 ADR**：ADR-002, ADR-006  
**优先级**：Must Have

## QAS-004：设备离线后仍可查询最后状态

| 要素 | 描述 |
| --- | --- |
| 刺激源 | IoT 设备 |
| 刺激 | 某设备停止上报数据超过离线阈值 |
| 环境 | 设备网络不稳定，云端服务正常运行 |
| 制品 | 设备影子、设备状态 API |
| 响应 | 系统将设备标记为 offline，并保留最后一次已知状态、最后上报时间和期望配置 |
| 响应度量 | 设备离线后，API 仍能在 1 秒内返回该设备最后一次已知状态 |

**关联 ADR**：ADR-004  
**优先级**：Must Have

## QAS-005：传感器历史数据按时间查询

| 要素 | 描述 |
| --- | --- |
| 刺激源 | 运维人员 |
| 刺激 | 查询某仓库过去 1 小时的温度变化 |
| 环境 | 系统积累了连续传感器遥测数据 |
| 制品 | TimescaleDB、遥测数据表、查询 API |
| 响应 | 系统按设备、仓库和时间范围过滤数据，并返回时间序列结果 |
| 响应度量 | 本地原型环境下，查询 1 小时窗口内的单设备数据响应时间不超过 2 秒 |

**关联 ADR**：ADR-003  
**优先级**：Should Have

## QAS-006：伪造设备接入被拒绝

| 要素 | 描述 |
| --- | --- |
| 刺激源 | 未授权客户端 |
| 刺激 | 客户端尝试使用不存在的设备 ID 或错误凭证发布 MQTT 消息 |
| 环境 | 系统开启设备身份校验策略 |
| 制品 | MQTT Broker、device-gateway、设备注册表 |
| 响应 | 系统拒绝未授权消息，记录安全事件，不更新设备影子 |
| 响应度量 | 未授权消息拒绝率为 100%，安全事件可在日志或 API 中查询 |

**关联 ADR**：ADR-001  
**优先级**：Should Have

## QAS-007：告警规则变更不影响设备接入

| 要素 | 描述 |
| --- | --- |
| 刺激源 | 开发人员或运维人员 |
| 刺激 | 将高温告警阈值从 38 摄氏度调整为 35 摄氏度 |
| 环境 | 设备持续上报数据，接入链路不能停机 |
| 制品 | 告警规则、inventory-service、alert-service |
| 响应 | 系统更新告警规则后，新阈值用于后续异常判断，device-gateway 继续接收设备消息 |
| 响应度量 | 规则变更过程中 MQTT 消息接入不中断，后续异常判断使用新阈值 |

**关联 ADR**：ADR-005  
**优先级**：Should Have

## QAS-008：批量设备升级不影响在线采集

| 要素 | 描述 |
| --- | --- |
| 刺激源 | 运维人员 |
| 刺激 | 对同一仓库的一批设备下发 OTA 升级任务 |
| 环境 | 仓库仍有在线设备持续上报数据 |
| 制品 | 设备影子、边缘网关、OTA 策略 |
| 响应 | 系统将 OTA 目标版本写入设备期望状态，并按批次控制升级，不阻塞其他设备数据采集 |
| 响应度量 | OTA 任务设计为批量分组执行，未参与当前批次的在线设备继续正常上报 |

**关联 ADR**：ADR-004, ADR-002  
**优先级**：Should Have

## QAS-009：库存低于阈值触发补货事件

| 要素 | 描述 |
| --- | --- |
| 刺激源 | 重量传感器或 RFID 扫码设备 |
| 刺激 | 某货架重量或库存计数低于补货阈值 |
| 环境 | 系统正常运行，库存规则已配置 |
| 制品 | inventory-service、库存状态、补货事件 |
| 响应 | 系统更新库存状态，并生成 REPLENISHMENT_REQUIRED 事件 |
| 响应度量 | 库存低于阈值后 10 秒内生成补货事件，事件包含 warehouseId、shelfId、skuId 和 currentQuantity |

**关联 ADR**：ADR-005  
**优先级**：Should Have
```

Expected: `Quality_Scenarios.md` contains at least 8 quality scenarios, covers at least 4 quality attributes, and uses the six-element format.

- [ ] **Step 2: Verify scenario count**

Run:

```powershell
$count = (Select-String -Path 'docs/01_adf/Quality_Scenarios.md' -Pattern '^## QAS-' | Measure-Object).Count
if ($count -lt 8) {
  "scenario count $count is below 8"
  exit 1
}
"quality scenarios ok: $count"
```

Expected: prints `quality scenarios ok: 9`.

- [ ] **Step 3: Verify six-element fields**

Run:

```powershell
$content = Get-Content 'docs/01_adf/Quality_Scenarios.md' -Raw -Encoding utf8
$fields = @('刺激源', '刺激', '环境', '制品', '响应', '响应度量')
$missing = $fields | Where-Object { $content -notlike "*| $_ |*" }
if ($missing) {
  $missing
  exit 1
}
'six element fields ok'
```

Expected: prints `six element fields ok`.

- [ ] **Step 4: Commit quality scenarios**

Run:

```powershell
git add docs/01_adf/Quality_Scenarios.md
git commit -m "docs: add quality attribute scenarios"
```

Expected: Git creates a commit named `docs: add quality attribute scenarios`.

## Task 4: Phase 1 Cross-Document Review

**Files:**
- Review: `README.md`
- Review: `docs/01_adf/ADF_Brief.md`
- Review: `docs/01_adf/Quality_Scenarios.md`
- Review: `docs/superpowers/specs/2026-06-02-iot-smart-warehouse-design.md`

- [ ] **Step 1: Check required traceability terms**

Run:

```powershell
$files = @(
  'README.md',
  'docs/01_adf/ADF_Brief.md',
  'docs/01_adf/Quality_Scenarios.md'
)
$terms = @(
  'MQTT',
  '边云协同',
  '设备影子',
  'TimescaleDB',
  '事件',
  '告警',
  '补传'
)
foreach ($file in $files) {
  $content = Get-Content $file -Raw -Encoding utf8
  foreach ($term in $terms) {
    if ($content -notlike "*$term*") {
      "$file missing $term"
      exit 1
    }
  }
}
'traceability terms ok'
```

Expected: prints `traceability terms ok`.

- [ ] **Step 2: Check for forbidden placeholders**

Run:

```powershell
$patterns = @('TO' + 'DO', 'TB' + 'D', '待' + '定', '占' + '位', 'x' + 'x' + 'x', 'X' + 'X' + 'X')
foreach ($pattern in $patterns) {
  $matches = Select-String -Path 'README.md','docs/01_adf/ADF_Brief.md','docs/01_adf/Quality_Scenarios.md' -Pattern $pattern -CaseSensitive:$false -Encoding utf8
  if ($matches) {
    $matches
    exit 1
  }
}
'no placeholders ok'
```

Expected: prints `no placeholders ok`.

- [ ] **Step 3: Inspect Git status**

Run:

```powershell
git status --short --ignored
```

Expected:

```text
?? 《软件体系结构》课程期末大作业 (2).pdf
!! .superpowers/
```

Committed project files should not appear as unstaged changes. The assignment PDF may remain untracked unless the user asks to commit it.

- [ ] **Step 4: Commit review fixes if needed**

If Steps 1 or 2 fail and files are edited, run:

```powershell
git add README.md docs/01_adf/ADF_Brief.md docs/01_adf/Quality_Scenarios.md
git commit -m "docs: refine phase 1 architecture drivers"
```

Expected: Git creates a commit only if review fixes were made.

## Self-Review Notes

- Spec coverage: This plan covers the confirmed design sections for project name, scope, quality priorities, core architecture direction, Phase 1 document requirements, and repository skeleton. ADRs, C4 views, ATAM, prototype services, and validation scripts are intentionally left for later implementation plans because they are separate deliverable phases.
- Placeholder scan: The plan contains no unresolved placeholder instructions.
- Type consistency: File paths, service names, ADR IDs, and QAS IDs are consistent with the approved design spec.
